import React, { useState } from "react";

// interface IProps {
// }
// export const : React.FC<IProps> = React.memo(({}) => {

//     return <></>;
// });
export default function Loading() {
    // You can add any UI inside Loading, including a Skeleton.
    return <h1>LOADING VALIDATIONS</h1>
}